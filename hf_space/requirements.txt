# P8 HuggingFace Space — Gradio shell over the FastAPI backend.
# Pure UI + PDF rendering — no model weights, no DB clients.
gradio>=5.14.0,<6
# Gradio 5.x ships its own compatible starlette and huggingface_hub;
# the 4.x compat pins are no longer needed.
huggingface_hub>=0.27
httpx>=0.27,<0.29
pdfplumber>=0.10,<0.12
Pillow>=10.0,<12

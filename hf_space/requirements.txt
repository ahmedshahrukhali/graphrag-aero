# P8 HuggingFace Space — Gradio shell over the FastAPI backend.
# Pure UI + PDF rendering — no model weights, no DB clients.
gradio>=4.40,<5
# Gradio 4.x's oauth.py imports HfFolder, which huggingface_hub 1.0 removed.
# Pin to the 0.x line so the import resolves.
huggingface_hub>=0.23,<1.0
httpx>=0.27,<0.29
pdfplumber>=0.10,<0.12
Pillow>=10.0,<12
